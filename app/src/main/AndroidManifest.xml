<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tj.chaersi.rxjavademo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".rxbinding.ButtonDemoActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SimpleActivity" />
        <activity android:name=".DataConvertActivity" />
        <activity android:name=".ThreadCtrlActivity" />
        <activity android:name=".SimpleSameActivity" />
        <activity android:name=".rxjava.ColorContainActivity" />
        <activity android:name=".rxjava.SelfAddActivity"></activity>
    </application>

</manifest>